# 🚀 Инструкция по настройке Webhook системы для мгновенной выдачи донатов

## Что это дает?
✅ **Мгновенная выдача** — донат выдается сразу после оплаты (без задержки)  
✅ **Автоматическая интеграция** — LuckPerms выдает права автоматически  
✅ **Безопасность** — проверка подписи запросов  

---

## 📋 Шаг 1: Настройка секретов на сайте

### 1.1 Добавьте секреты в проект poehali.dev:

**MINECRAFT_WEBHOOK_URL**
```
http://ВАШ_IP_СЕРВЕРА:8080/webhook
```
Замените `ВАШ_IP_СЕРВЕРА` на реальный IP вашего Minecraft сервера.

**MINECRAFT_WEBHOOK_SECRET**
```
ваш-секретный-ключ-2024
```
Придумайте любой сложный ключ (например: `mc_donate_webhook_xyz123`)

---

## 📦 Шаг 2: Установка плагина на сервер

### 2.1 Скомпилируйте плагин

Перейдите в папку `minecraft-plugin/DonateWebhook/` и выполните:

```bash
mvn clean package
```

Готовый плагин будет в `target/DonateWebhook.jar`

**Или скачайте готовый:** [здесь будет ссылка на релиз]

### 2.2 Установите плагин

1. Скопируйте `DonateWebhook.jar` в папку `plugins/` на сервере
2. Убедитесь, что установлен **LuckPerms** (версия 5.4+)
3. Перезапустите сервер

---

## ⚙️ Шаг 3: Настройка плагина

### 3.1 Откройте `plugins/DonateWebhook/config.yml`

```yaml
webhook:
  port: 8080  # Порт для webhook (откройте в файрволе!)
  secret: "ваш-секретный-ключ-2024"  # Тот же, что в MINECRAFT_WEBHOOK_SECRET

notifications:
  notify_admins: true  # Уведомления админам о донатах
  log_to_console: true  # Логи в консоль
```

### 3.2 Откройте порт в файрволе

**Linux (iptables):**
```bash
sudo iptables -A INPUT -p tcp --dport 8080 -j ACCEPT
sudo iptables-save
```

**Linux (ufw):**
```bash
sudo ufw allow 8080/tcp
```

**Windows Server:**
```
Панель управления → Брандмауэр → Разрешить приложение → Добавить порт 8080
```

---

## 🔧 Шаг 4: Настройка LuckPerms групп

Создайте группы для каждого донат-пакета:

```bash
# Survival режим
/lp creategroup beginner
/lp creategroup premium
/lp creategroup vip
/lp creategroup legend

# Anarchy режим
/lp creategroup anarchist
/lp creategroup warlord
/lp creategroup chaos
```

Настройте права для каждой группы в LuckPerms (или используйте существующие).

---

## ✅ Шаг 5: Проверка работы

### 5.1 Перезапустите плагин
```
/reload confirm
```

### 5.2 Проверьте логи
В консоли должно быть:
```
[DonateWebhook] Webhook сервер запущен на порту 8080
[DonateWebhook] LuckPerms подключен!
```

### 5.3 Тестовый донат

1. Перейдите на сайт доната
2. Выберите пакет и заполните форму
3. Нажмите "Задонатить"
4. После "оплаты" донат должен выдаться МГНОВЕННО

---

## 🐛 Решение проблем

### Проблема: "Webhook сервер не запустился"
- Проверьте, что порт 8080 не занят: `netstat -tuln | grep 8080`
- Попробуйте другой порт в `config.yml`

### Проблема: "Донат не выдается"
- Проверьте логи плагина: `logs/latest.log`
- Убедитесь, что секретный ключ совпадает в сайте и плагине
- Проверьте, что порт открыт в файрволе

### Проблема: "LuckPerms не найден"
- Убедитесь, что LuckPerms установлен и загружен
- Проверьте версию: `/lp info` (должна быть 5.4+)

### Проблема: "Invalid signature"
- Секретный ключ не совпадает
- Проверьте `MINECRAFT_WEBHOOK_SECRET` на сайте и `secret` в config.yml

---

## 📊 Как это работает?

```
[Сайт] Игрок оплачивает донат
   ↓
[Backend] Сохраняет в БД и МГНОВЕННО отправляет webhook
   ↓
[Minecraft Сервер] Плагин принимает запрос на :8080/webhook
   ↓
[LuckPerms] Выдает права и группу игроку
   ↓
[Игрок] Получает донат БЕЗ ЗАДЕРЖКИ!
```

---

## 🔐 Безопасность

✅ Webhook подписывается HMAC-SHA256  
✅ Проверка подписи на стороне сервера  
✅ Только ваш сайт может отправлять валидные запросы  

**Важно:** Не публикуйте секретный ключ в открытом доступе!

---

## 📞 Поддержка

Если что-то не работает:
1. Проверьте логи плагина
2. Проверьте логи backend функции (в poehali.dev)
3. Убедитесь, что порт открыт и доступен извне

Готово! Теперь донаты выдаются мгновенно! 🎉
