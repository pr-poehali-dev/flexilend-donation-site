# 🎮 DonateWebhook - Плагин для мгновенной выдачи донатов

## 📦 Готовый плагин

**К сожалению, я не могу скомпилировать Java код напрямую**, но у вас есть несколько вариантов:

---

## ✅ Вариант 1: Компиляция на вашем компьютере (5 минут)

### Требования:
- Java 17+ установлена
- Maven установлен

### Команды:
```bash
cd minecraft-plugin/DonateWebhook/
mvn clean package
```

Готовый плагин: `target/DonateWebhook.jar` ✅

---

## ✅ Вариант 2: Онлайн компиляция (без установки программ)

Используйте **GitHub Actions** для автоматической компиляции:

1. Подключите GitHub к проекту
2. Создайте файл `.github/workflows/build.yml`:

```yaml
name: Build Plugin
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'
      - name: Build with Maven
        run: |
          cd minecraft-plugin/DonateWebhook
          mvn clean package
      - uses: actions/upload-artifact@v3
        with:
          name: DonateWebhook
          path: minecraft-plugin/DonateWebhook/target/DonateWebhook.jar
```

3. Плагин скомпилируется автоматически и будет доступен в разделе "Actions"

---

## ✅ Вариант 3: Облачная IDE

Используйте **Gitpod** или **GitHub Codespaces**:

1. Откройте проект в браузерной IDE
2. В терминале выполните:
```bash
cd minecraft-plugin/DonateWebhook/
mvn clean package
```
3. Скачайте готовый .jar файл

---

## 📋 Что входит в плагин:

✅ Webhook сервер на порту 8080  
✅ Интеграция с LuckPerms  
✅ Поддержка всех донат-пакетов (Survival + Anarchy)  
✅ Проверка подписи запросов (безопасность)  
✅ Уведомления администраторам  
✅ Мгновенная выдача прав после оплаты  

---

## 🔧 Установка:

1. Скомпилируйте плагин одним из способов выше
2. Скопируйте `DonateWebhook.jar` в папку `plugins/` на сервере
3. Убедитесь что установлен **LuckPerms 5.4+**
4. Настройте `plugins/DonateWebhook/config.yml`
5. Откройте порт 8080 в файрволе
6. Перезапустите сервер

Подробная инструкция: **WEBHOOK_SETUP_GUIDE.md**

---

## 🆘 Нужна помощь с компиляцией?

Напишите в чат — помогу пройти процесс пошагово!
